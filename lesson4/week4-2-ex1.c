#include <stdio.h>
#include <math.h>

int main()
{
    int a = 16;
    float b = 15.5;
    printf("%d", sqrt(a));
    printf("%f", ceil(b));
    printf("%f", floor(b));
    printf("%d", pow(a, 1.5));
    printf("%d",  exp(a));
}